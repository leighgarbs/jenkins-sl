#!/bin/bash -e

# All stages should change directory to here
cd $STAGE_DIR

git clean -x -d -f
scan-build ../bin/run-cmake --debug .
scan-build -o clangScanBuildReports -v -v --use-cc clang --use-analyzer=/usr/bin/clang make -B tests
